/* Minimum of two numbers using conditional operator */
public class min_conditional {
    public static void main (String args[])
    {
                int num1 = 8;
                int num2 = 100;
                int result = (num1 < num2) ? num1 : num2;
                System.out.println("Minimum between two numbers is:" +result);
    }
}
